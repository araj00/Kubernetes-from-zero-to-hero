# just copy paste this code into any node into /etc/kubernetes/manifests and node will automatically place the pod using kubelet as a static pod
apiVersion: v1
kind: Pod
metadata:
  name: my-nginx-2
spec:
  nodeName: kind-control-plane
  containers:
  - name: nginx
    image: nginx:1.19
    ports:
      - containerPort: 80
