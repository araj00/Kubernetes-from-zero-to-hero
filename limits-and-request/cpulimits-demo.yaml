apiVersion: v1
kind: Pod
metadata:
  name: cpu-demo
spec:
  containers:
  - name: cpu-container
    image: vish/stress
    resources:
      limits:
        cpu: "900m"
      requests:
        cpu: "500m"
    args:
     - -cpus
     - "1"
